# ==============================================================================
# Backend — DO NOT create a local .env here.
# All environment variables are loaded from the root monorepo .env:
#
#   d:/Java/Bank/.env
#
# Run with:
#   Windows:  dev.bat
#   Any:      dotenvx run --env-file=../.env -- ./gradlew.bat bootRun
#
# See docs/ENVIRONMENT.md for the full variable reference.
# ==============================================================================
