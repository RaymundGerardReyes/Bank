spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: raymundgerardrestaca@gmail.com
    password: agonpyxmthgztxbf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: hardened-banking-backend
  output:
    ansi:
      enabled: always
  main:
    allow-bean-definition-overriding: false

server:
  port: ${PORT:8085}
  address: 0.0.0.0  # <-- ADD THIS EXACT LINE
  servlet:
    context-path: /
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never
    include-exception: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
