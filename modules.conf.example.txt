# Example modules.conf for a medical emergency detection system
# Format: scope|tag_prefix|commit_type|commit_description|path1,path2,...
# Edit the scope names, paths, and descriptions to match YOUR repo layout.
# Lines starting with # are ignored.

# --- ML / Computer Vision pipeline ---
inference-engine|inference-engine|feat|add CNN-LSTM hybrid model for pose estimation and fall detection|src/ml/models,src/ml/inference
dataset-pipeline|dataset-pipeline|fix|correct augmentation bug in training dataloader|src/ml/data,src/ml/preprocessing
rtsp-streaming|rtsp-streaming|feat|integrate RTSP camera ingestion with YOLOv11 detector|src/ml/streaming

# --- Backend (ASP.NET Core) ---
backend-api|backend-api|fix|harden alert endpoint against malformed payloads|backend/src/Controllers,backend/src/Services
backend-alerts|backend-alerts|feat!|replace polling alert delivery with SignalR push notifications|backend/src/Alerts

# --- Frontend (Angular/React) ---
dashboard-ui|dashboard-ui|feat|add real-time emergency alert dashboard view|frontend/src/features/dashboard
auth-ui|auth-ui|fix|fix session timeout redirect loop|frontend/src/features/auth

# --- Infra / Docker / DevOps ---
infra|infra|chore|update docker-compose for GPU-enabled inference container|docker-compose.yml,Dockerfile.inference

# --- Docs ---
project-docs|docs-project|docs|update thesis architecture and system design notes|docs/architecture.md,docs/thesis-notes.md
